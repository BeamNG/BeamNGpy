[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--verbose"
norecursedirs = [
    "dist",
    "build",
    ".tox",
]
filterwarnings = [
    "ignore::UserWarning",
]
